# 版本说明

## 1.0.1

`1.0.1` 是登录自动化和发布体验修复版，重点解决“点击登录后一直显示登录中、登录页不自动填账号密码”的问题。

### 升级内容

- 新增账号行里的“登录”能力：点击后打开该条目的网址。
- 如果打开后已经是首页、控制台、工作台等已登录页面，自动标记为登录成功。
- 如果打开后是标准登录表单，自动填入用户名和密码，并执行一次提交。
- 如果遇到验证码、OTP、账号选择、授权确认等不确定流程，会停止自动操作，交给用户手动继续。
- 同一个账号重复点击登录时，会优先聚焦已有登录标签页，避免重复开页。
- 支持多个不同账号并行登录，各自保持独立状态。
- 登录后保存密码提示的等待时间进入系统设置，默认 `5s`，范围 `1s` 到 `15s`。
- 修复保存密码提示误弹问题：只有表单提交后检测到登录成功，才弹出“保存到 Password WebDAV？”。
- 修复 content script 构建格式：Chrome content script 现在会被打包成无顶层 `import/export` 的经典单文件脚本。
- 新增构建校验，防止 content script 再次被错误打包成 module。
- 新增 tag 发布流程：推送 `v*` tag 后自动构建扩展并生成 GitHub Release zip。

### 已验证

- 单元测试：`npm test`
- 构建：`npm run build`
- 真实扩展回归：
  - 打开已登录首页后，任务状态会变为 `success / already_logged_in`。
  - 打开登录页后，会自动填入账号密码并提交到目标页面。

## 1.0.0

`1.0.0` 是 Password WebDAV 的第一个可用版本，定位是一个只保留 Chrome 插件形态、后端直接使用 WebDAV 的个人密码管理工具。

### 核心机制

- 不需要单独后端服务，WebDAV 只负责存储加密后的 vault 文件。
- WebDAV 根目录固定为 `PasswordWebDAV/`，设置里填写的是它后面的子路径，例如 `password-vault.json` 或 `work/password-vault.json`。
- vault 使用主密码解锁；主密码不会上传到 WebDAV，也不会保存到长期本地配置。
- WebDAV 文件中保存的是密文 JSON，包含 `salt`、KDF 参数、加密后的 vault key 和加密后的 vault 内容。
- 解锁后，明文 vault 和当前会话主密码只临时保存在 `chrome.storage.session`，锁定插件或关闭浏览器后需要重新解锁。

### 功能说明

- Chrome MV3 插件安装、配置、解锁或创建 WebDAV vault。
- 新增、编辑、搜索、删除密码条目。
- 支持标题、网址、用户名、密码、文件夹、标签、备注等字段。
- 主界面支持文件夹视图和全部账号视图切换。
- 支持树形文件夹和子文件夹，例如 `工作/GitHub`。
- 支持创建、删除、重命名、拖拽移动文件夹。
- 支持将账号拖拽到文件夹中完成分类。
- 删除文件夹时，原文件夹及子文件夹里的账号会移动到未分类，不会跟着删除。
- 列表里可直接删除密码条目。
- 密码默认以 `********` 掩码展示，点击眼睛图标后临时显示。
- 支持复制账号、复制密码、手动填充当前页面。
- 输入账号时会按当前域名和输入内容提示可用账号。
- 保存密码时可选择文件夹并编辑标题。
- 支持主题设置和 `zh/en` 语言切换。
- WebDAV 保存时会自动创建缺失的父目录。

### 使用限制

- 插件不能直接读取 Google Password Manager 里已经保存的密码；如需迁移，后续需要走浏览器导出再导入的路线。
- 对非常规登录页、第三方 iframe、验证码、OTP、多步骤 SSO 等复杂登录流程，可能需要用户手动继续。
- 当前没有多设备冲突合并机制，多端同时编辑时以后保存的一端可能覆盖先保存的一端。
